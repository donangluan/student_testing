<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="org.example.student_testing.test.mapper.AnswerOptionMapper">
    <select id="findAll" resultType="org.example.student_testing.test.dto.AnswerOptionDTO">
        SELECT option_code AS optionCode, option_label AS optionLabel FROM answer_options ORDER BY option_code
    </select>

    <select id="findByCode" parameterType="string" resultType="org.example.student_testing.test.dto.AnswerOptionDTO">
        SELECT option_code AS optionCode, option_label AS optionLabel FROM answer_options WHERE option_code = #{code}
    </select>
</mapper>
