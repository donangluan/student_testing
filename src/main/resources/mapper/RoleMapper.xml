<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="org.example.student_testing.student.mapper.RoleMapper">

    <select id="findRoleIdByName" resultType="int">
        SELECT role_id
        FROM roles
        WHERE role_name = #{roleName}
    </select>

    <select id="findAllRoles" resultType="org.example.student_testing.student.entity.Role">
        SELECT role_id, role_name FROM roles ORDER BY role_id
    </select>

    <select id="findRoleById" resultType="org.example.student_testing.student.entity.Role">
        SELECT role_id, role_name FROM roles WHERE role_id = #{roleId}
    </select>

</mapper>
