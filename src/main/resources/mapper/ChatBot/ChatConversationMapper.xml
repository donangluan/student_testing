<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="org.example.student_testing.chatbot.mapper.ChatConversationMapper">
    <insert id="insertConversation">
        INSERT INTO chat_conversations(id,username, created_at)
        VALUES (#{id},  #{username}, NOW())
            ON CONFLICT (id) DO NOTHING
    </insert>

</mapper>