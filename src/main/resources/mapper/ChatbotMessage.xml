<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.example.student_testing.test.mapper.ChatbotMapper">
    <insert id="insertMessage">
        INSERT INTO chatbot_message (username, role, message, response)
        VALUES (#{username}, #{role}, #{message}, #{response})
    </insert>

    <select id="getMessagesByUser" resultType="org.example.student_testing.test.dto.ChatbotMessageDTO">
        SELECT username, role, message, response
        FROM chatbot_message
        WHERE username = #{username}
        ORDER BY created_at DESC
    </select>
</mapper>