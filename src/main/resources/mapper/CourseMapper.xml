<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="org.example.student_testing.student.mapper.CourseMapper">

    <select id="findAll" resultType="org.example.student_testing.student.entity.Course">
        SELECT course_id AS courseId,
               course_name AS courseName,
               teacher_id AS teacherId,
               created_at AS createdAt,
               updated_at AS updatedAt
        FROM courses
    </select>


    <select id="findByCourseId" parameterType="int" resultType="org.example.student_testing.student.entity.Course">
        SELECT course_id AS courseId,
               course_name AS courseName,
               teacher_id AS teacherId,
               created_at AS createdAt,
               updated_at AS updatedAt
        FROM courses
        WHERE course_id = #{courseId}
    </select>


    <insert id="create" parameterType="org.example.student_testing.student.entity.Course">
        INSERT INTO courses (course_name, teacher_id, created_at, updated_at)
        VALUES (#{courseName}, #{teacherId}, NOW(), NOW())
    </insert>


    <update id="update" parameterType="org.example.student_testing.student.entity.Course">
        UPDATE courses
        SET course_name = #{courseName},
            teacher_id = #{teacherId},
            updated_at = NOW()
        WHERE course_id = #{courseId}
    </update>


    <delete id="delete" parameterType="int">
        DELETE FROM courses
        WHERE course_id = #{courseId}
    </delete>


    <select id="findCourseNameById" parameterType="int" resultType="String">
        SELECT course_name FROM courses WHERE course_id = #{courseId}
    </select>

</mapper>
